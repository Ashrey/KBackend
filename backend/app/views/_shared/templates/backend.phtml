<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="<?php echo APP_CHARSET ?>" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><?php echo Config::get('backend.app.nombre') ?></title>
        <?php
        Tag::css('bootstrap.min');
        echo Html::includeCss();
        ?>
    </head>
    <body>
        <?php View::partial('backend/header') ?>
        <div class="container">
            <div id="contenido" class="content">
                <?php View::content(); ?>
            </div>
        </div>
        <?php View::partial('backend/pie') ?>
        <?php
        echo Tag::js('zepto.kumbiaphp');
        echo Tag::js('bootstrap');
        ?>
    </body>
</html>
